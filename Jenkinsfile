pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo "Hello jbnnb"'
		sh '''
			echo "Run command ls"
			ls -a
		'''
            }
        }
    }
}
